<div class="event-element">
  <h3><%= event.name %></h3>
  <strong>Place : </strong> <%= event.venue.name %>
  <br>
  <strong>Date : </strong><%= event.date.strftime('%A, %d %b %Y %l:%M %p') %>
</div>
